services:
  - type: web
    name: spaced-retention-api
    runtime: python
    buildCommand: pip install -r requirements-api.txt
    startCommand: gunicorn -w 4 -b 0.0.0.0:5000 tracker_api:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
